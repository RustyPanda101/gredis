# Build directories
build/
build-*/

# Test / fuzz artifacts
tests/fuzz/artifacts/
*.profraw
core
core.*

# Benchmark output (results live in docs/benchmarks.md, not committed here)
bench/results/

# Snapshot files produced by manual runs
*.snap
*.snap.tmp

# Python bytecode cache from running the integration tests
__pycache__/
*.pyc

# Editor / OS cruft
.vscode/
.idea/
*.swp
.DS_Store

# compile_commands.json (regenerated by CMake, not source)
compile_commands.json

# Early scratch experiments, not part of the gredis build.
learning/
